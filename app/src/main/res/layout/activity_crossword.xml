<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#121212"
    android:padding="16dp"
    android:gravity="center_horizontal">

    <!-- رأس اللعبة (عرض المستوى والنتيجة) -->
    <TextView
        android:id="@+id/tvLevelTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="المستوى الأول"
        android:textSize="22sp"
        android:textColor="#FFFFFF"
        android:textStyle="bold"
        android:layout_marginBottom="16dp"/>

    <!-- مربع التلميح أو السؤال الحالي -->
    <TextView
        android:id="@+id/tvClueDisplay"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="السؤال يظهر هنا..."
        android:textSize="16sp"
        android:textColor="#FFEB3B"
        android:gravity="center"
        android:padding="12dp"
        android:background="#1E1E1E"
        android:layout_marginBottom="20dp"/>

    <!-- شبكة الكلمات المتقاطعة (تتولد برمجياً حسب حجم الشبكة مثلاً 8x8) -->
    <GridLayout
        android:id="@+id/crosswordGrid"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:columnCount="8"
        android:rowCount="8"
        android:layout_gravity="center"/>

    <!-- زر التحقق من الإجابات -->
    <Button
        android:id="@+id/btnCheckAnswers"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="تحقق من الحل"
        android:backgroundTint="#4CAF50"
        android:textColor="#FFFFFF"
        android:layout_marginTop="30dp"/>

</LinearLayout>
